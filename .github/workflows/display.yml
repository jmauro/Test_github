---
name: "Display test"

on:
  workflow_dispatch:

env:
  TRUNCATED_REF: ${{ github.ref_name[0:7] }}

run-name: Deploy - ${{ env.TRUNCATED_REF }}

jobs:
  display_test:
    runs-on: ubuntu-latest
    steps:
      - name: test display
        shell: bash
        run: |
          echo "::notice title=Selected chart::kube-system"
          echo "::notice ::Selected chart 'kube-system'"
